[project]
name = "supercode"
version = "0.1.0"
description = "Multi-Agent AI Development Tools"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}

[tool.uv]
managed = true

[tool.uv.workspace]
members = ["packages/*"]
