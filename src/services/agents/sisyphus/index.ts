/**
 * Sisyphus Module
 * Dynamic prompt builder for the orchestrator agent.
 */

export {
  buildOrchestratorPrompt,
  buildDelegationTable,
  buildKeyTriggersSection,
  buildToolSelectionTable,
  buildExplorationSection,
  buildSpecialistSection,
  buildAdvisorSection,
  buildAgentSection,
  buildHardBlocksSection,
  buildAntiPatternsSection,
  buildDelegationPromptStructure,
  collectAgentMetadata,
  type CollectedAgentMetadata,
} from "./prompt-builder";
